{
  "name": "hololis-craft-mythicmobs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "format": "prettier --write \"packs/**/*.{yml,yaml}\"",
    "format:check": "prettier --check \"packs/**/*.{yml,yaml}\"",
    "prepare": "husky",
    "lint-staged": "lint-staged",
    "install:recommended-vscode-extensions": "for ext in $(jq -r '.recommendations[]' .vscode/extensions.json); do code --install-extension $ext; echo \"\"; done"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1"
  }
}
